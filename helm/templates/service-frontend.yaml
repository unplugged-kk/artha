apiVersion: v1
kind: Service
metadata:
  name: artha-frontend-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "artha.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    {{- include "artha.frontend.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      protocol: TCP
      appProtocol: http
      targetPort: http
      name: web
